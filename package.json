{
  "name": "tonal",
  "version": "5.5.0",
  "description": "Two-way tone translator Chrome Extension",
  "main": "background.js",
  "scripts": {
    "deploy": "cd src/backend && wrangler deploy",
    "dev": "cd src/backend && wrangler dev",
    "zip": "powershell compress-archive -Path manifest.json, src, icons -DestinationPath tonal-v2.1.0.zip",
    "kill": "npx kill-port 3000 3001 3002 8080",
    "save": "git add -A && git commit -m \"checkpoint: [timestamp]\" && git push"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
